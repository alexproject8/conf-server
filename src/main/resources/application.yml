server:
  port: 8888
  
spring:
  #Spring Cloud Config Server Repository
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexproject8/arch-conf.git
          default-label: conf
          search-paths:
          - '*service'